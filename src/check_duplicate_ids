cd gdf
> /tmp/temp_id_file
./show_group_ids.sh | uniq -c |awk '{if($1 > 1) {print $0}}' 2>/dev/null > /tmp/temp_id_file

if [ -s /tmp/temp_id_file ]; then
  echo "Duplicate IDs :- `cat /tmp/temp_id_file | awk '{ORS=" "} {print $2}'`"
else
  echo 0 
fi
cd ../
rm /tmp/temp_id_file
