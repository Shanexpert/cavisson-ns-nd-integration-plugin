# Name      : nsu_copy_flow
# Author    : Saloni Tyagi
# Purpsose  : copy flow file from source script to destination script.
# Usage     : nsu_copy_flow <-s proj/subProject/script Name> <-d > <-D to enable debug log>   
# e.g.  
#
# Modification History:
# Previously Shell program was handling various operations, Now ScriptCopyFlow is taking care of all operations
# 12/01/12: Saloni - Initial version
#!/bin/sh
USER_NAME=`whoami 2>/dev/null`
if [ $? != 0 ];then
  echo "Error: Unable to get the real user name"
  exit 1
fi

#Check and get script type and call appropriate class
if [ -d /home/cavisson/monitors ];then
  java -cp $NS_WDIR/webapps/netstorm/WEB-INF/lib/netstorm_bean.jar:/home/cavisson/monitors/lib/java-getopt-1.0.9.jar -DNS_WDIR=$NS_WDIR pac1.Bean.ScriptCopyFlow $* -u $USER_NAME
else
  java -cp $NS_WDIR/webapps/netstorm/WEB-INF/lib/netstorm_bean.jar:/opt/cavisson/monitors/lib/java-getopt-1.0.9.jar -DNS_WDIR=$NS_WDIR pac1.Bean.ScriptCopyFlow $* -u $USER_NAME
fi
