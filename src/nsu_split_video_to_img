while getopts p:f:i: option
do
case "${option}"
in
p) VIDEO_FILE_PATH=${OPTARG};;
f) FRAME_RATE=${OPTARG};;
i) IMAGE_DIR=${OPTARG};;
esac
done

ffmpeg -i $VIDEO_FILE_PATH -vf fps=$FRAME_RATE $IMAGE_DIR/%d.jpg
