# Name      : nsu_copy_script
# Author    : Neha Rastogi
# Purpsose  : copy script directory from source script to destination path.
# Usage     : nsu_copy_script <-s proj/subProject/script Name> <-n proj/subProject/script Name > <-D to enable debug log>   
# Modification History:
# Previously Shell program was handling various operations, Now ScriptCopyUtils is taking care of all operations
# 15/10/12: Neha - Initial version
#!/bin/sh
USER_NAME=`whoami 2>/dev/null`
if [ $? != 0 ];then
  echo "Error: Unable to get the real user name"
  exit 1
fi

 #Check and get script type and call appropriate class
if [ -d /home/cavisson/monitors ];then
  java -cp $NS_WDIR/webapps/netstorm/WEB-INF/lib/netstorm_bean.jar:/home/cavisson/monitors/lib/java-getopt-1.0.9.jar -DNS_WDIR=$NS_WDIR pac1.Bean.ScriptCopyUtils $* -u $USER_NAME
else
  java -cp $NS_WDIR/webapps/netstorm/WEB-INF/lib/netstorm_bean.jar:/opt/cavisson/monitors/lib/java-getopt-1.0.9.jar -DNS_WDIR=$NS_WDIR pac1.Bean.ScriptCopyUtils $* -u $USER_NAME
fi
